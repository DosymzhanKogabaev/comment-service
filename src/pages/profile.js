import React from "react";
import useAuthRedirect from "../hooks/useAuthRedirect";

const Profile = () => {
    useAuthRedirect()
    return (
        <div>

        </div>
    )
}
export default Profile